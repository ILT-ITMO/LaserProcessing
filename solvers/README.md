# Solvers & Physical Modeling Module

## 📋 Обзор

Директория `solvers` является вычислительным ядром проекта. Здесь собраны физические модели и алгоритмы оптимизации, которые генерируют синтетические данные для обучения нейросетей (PINN) и решают задачи контактной механики.

Модуль разделен на три ключевые подсистемы:
1.  **🌡️ Temperature Solver:** Моделирование распространения тепла при лазерном воздействии.
2.  **⚙️ GFMD Tribology:** Солвер контактной механики (метод GFMD).
3.  **📉 Surface Optimizer:** Оптимизация топологии поверхности с использованием эволюционных алгоритмов.

---

## 1. Temperature Solver (Тепловая задача)
📂 **Расположение:** [`/solvers/for_temperature`](https://github.com/ILT-ITMO/LaserProcessing/tree/main/solvers/for_temperature)

C++ солвер для решения нестационарного уравнения теплопроводности.

### Назначение
Основная задача модуля — генерация **синтетических данных** (температурных полей), которые используются для дообучения (fine-tuning) Physics-Informed Neural Networks (PINN). Солвер моделирует нагрев материала лазерным лучом, учитывая параметры сканирования.

### Ключевые файлы
| Файл | Описание |
|------|----------|
| `minimarker.cpp` | Основной исполняемый код солвера. Реализует конечно-разностную схему или аналитическое решение для подвижного источника тепла. |

---

## 2. GFMD Tribology (Контактная механика)
📂 **Расположение:** [`/solvers/gfmd`](https://github.com/ILT-ITMO/LaserProcessing/tree/main/solvers/gfmd)

Реализация метода молекулярной динамики функции Грина (Green's Function Molecular Dynamics — GFMD) в континуальной формулировке для моделирования контактной механики.

### 🧪 Описание метода
Это эталонная реализация для плоского двумерного упругого полупространства, подвергаемого статическому вдавливанию жестким зондом (индентором) без трения с заданным профилем высоты.
Используется для инжиниринга контакта между линейно структурированной и гладкой поверхностями.

### 📤 Выходные данные
В результате численного моделирования рассчитываются две переменные состояния системы:
1. **Распределение нормальной силы** вдоль поверхности (в задаче жесткого штампа).
2. **Функция высоты** штампа и перемещение поверхности.

### 🛠 Требования и Сборка

Для работы с программой необходимо:
*   Unix-подобная ОС (Linux/macOS).
*   Библиотека **FFTW** (Fastest Fourier Transform in the West). *Протестировано на v3.3.7*.
*   Компилятор **Clang/LLVM**. *Протестировано на v3.8.1*.
*   GNU **Make**. *Протестировано на v4.1*.

**Сборка проекта:**
Выполните команду `make` в рабочей директории:
```bash
cd solvers/gfmd
make
```

---

## 3. Surface Optimizer (Оптимизация поверхности)
📂 **Расположение:** [`/solvers/surface_optimizer`](https://github.com/ILT-ITMO/LaserProcessing/tree/main/solvers/surface_optimizer)

Python-модуль для оптимизации профиля поверхности в задачах 2D упругого контакта.

### 🎯 Цель
Найти оптимальный профиль жесткой поверхности, который минимизирует комбинированную целевую функцию:
*   🔽 Минимизация пикового напряжения на границе раздела (interfacial stress).
*   🔼 Максимизация контактной жесткости (contact stiffness).

### 🧠 Алгоритм
*   Оптимизирует коэффициенты Фурье, определяющие периодический профиль поверхности.
*   Использует эволюционный алгоритм **CMA-ES** (Covariance Matrix Adaptation Evolution Strategy).
*   В качестве физического ядра использует GFMD солвер.

### 📦 Требования
*   Python 3.7+
*   NumPy
*   PyTorch (для GFMD солвера)
*   Matplotlib (для визуализации)
*   `cmaes` (установка: `pip install cmaes`)

### 🚀 Запуск и использование

**Быстрый старт:**
Запустите пример оптимизации:
```bash
python example_optimize_surface.py
```

**Как это работает:**
1. Скрипт выполняет несколько попыток оптимизации (trials) с различной случайной инициализацией.
2. Результаты сохраняются в директорию (например, `results/w=0.4`).
3. Для каждого запуска генерируются графики:
    *   История сходимости (Convergence history).
    *   Финальный профиль поверхности.
    *   Распределение контактных напряжений.

### ⚙️ Настройка параметров
Основные параметры находятся внутри `example_optimize_surface.py`:

| Параметр | Описание |
|----------|----------|
| `num_weights` | Размерность оптимизации (количество коэффициентов Фурье). |
| `stress_weight` | Баланс между целями (напряжение vs жесткость). |
| `num_trials` | Количество независимых запусков с разных стартовых точек. |
| `num_points` | Разрешение дискретизации поверхности. |

> **Примечание:** Оптимизация использует безразмерные единицы. См. комментарии в коде для формул конвертации в физические величины.

### Файловая структура модуля
*   `batch_optimize.py` — Скрипт для пакетного запуска оптимизации.
*   `mlwrapper1.py` — Обертка для взаимодействия ML-алгоритмов с физическим солвером.
*   `optimize_surface.py` — Основная логика оптимизации.

---

## 🤝 Взаимосвязь модулей

```
Temperature Solver
    ↓
    Синтетика: T(x,y,t)
    ↓
PINN Training

Surface Optimizer
    ↓
    Запрос расчета
    ↓
GFMD Solver
    ↓
    Напряжения и деформации
    ↓
Surface Optimizer
    ↓
    Оптимальный профиль
    ↓
Рекомендации для лазерной обработки
```


---

## 📞 Контакты

Разработка модулей солверов ведется в рамках проекта LaserProcessing. По вопросам физических моделей обращайтесь к контрибьюторам репозитория.
