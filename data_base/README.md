# Database Module: Laser Ablation Dataset

## 📋 Описание

Модуль `data_base` отвечает за сбор, обработку и структурирование экспериментальных данных по лазерной абляции материалов. Здесь реализованы алгоритмы для анализа микроскопических изображений, профилометрических измерений и подготовки полной базы данных режимов лазерного воздействия для обучения нейросетевых моделей.

Датасет содержит порядка **1000 размеченных образцов** экспериментов по абляции титана при различных режимах лазерного воздействия, включая микрофотографии, характеристики треков и измерения глубины воздействия.

---

## 🗂 Структура и назначение

### Материал датасета
- **Материал:** Титан (Titanium)
- **Лазерный источник:** YB:YAG T=100нс λ = 1064 нм
- **Количество образцов:** 4000+ размеченных экспериментов | по 4 сэмпла каждого режима
- **Хранилище:** [Hugging Face Hub](https://huggingface.co/datasets/maximmikhalevich/laser_ablation_of_titanium_4)

### Состав каждого сэмпла

Каждый образец в датасете включает:

| Поле | Описание |
|------|---------|
| **laser_parameters** | Режимы лазерного воздействия (мощность, длительность, частота, скорость развертки и т.д.) |
| **microscope_image** | Микрофотография полученного рельефа (в формате JPG/PNG) |
| **track_width** | Ширина лазерного трека, определенная автоматической сегментацией |
| **avg_width** | Усредненная ширина трека по слоям (более надежный показатель) |
| **width_std_dev** | Стандартное отклонение ширины для оценки вариабельности |
| **track_depth** | Глубина воздействия, измеренная профилометром |

---

## 🛠 Компоненты и инструменты

### 1. Сегментация микроскопических изображений

#### `segmentation_1.py`
Базовый алгоритм автоматического определения ширины лазерного трека на микрофотографии.

**Функциональность:**
- Автоматическое определение границ лазерного трека
- Вычисление ширины, длины трека
- Расчет усредненной ширины по слоям (слой = линия шириной 1 пиксель)
- Вычисление стандартного отклонения ширины
- Применение маски для визуализации результатов

**Принцип работы:**
1. Загрузка изображения в 8-битном формате
2. Преобразование в черно-белый формат
3. Вычисление порога сегментации (i-й процентиль яркости, обычно 25%)
4. Преобразование в битовое представление (0 = черный, 255 = белый)
5. Применение нижнего порога сегментации
6. Определение контуров крупных участков со значением 255
7. Вычисление метрик ширины и стандартного отклонения

**Валидация:**
- Протестирован на микроскопе с погрешностью **< 1%**
- Сравнение с оптическими измерениями показало высокую точность
- ⚠️ **Важно:** Использовать `avg_width` вместо `Width` для неровных краев трека

**Установка зависимостей:**
```bash
pip install opencv-python pillow numpy
```

**Использование:**
```python
from segmentation_1 import segmentation

# Загрузка и обработка изображения
seg = segmentation(image_path, percentile_threshold=25)
width = seg.avg_width
depth = seg.calculate_depth()
mask = seg.get_mask()
```

---

#### `mass_segmentation.py`
Алгоритм массовой сегментации множества микрофотографий с интерактивной подстройкой параметров.

**Функциональность:**
- Обработка большого количества изображений в пакетном режиме
- Интерактивная подстройка порога сегментации в процессе обработки
- Экспорт результатов в структурированный формат
- Визуализация прогресса и результатов

**Использование:**
```python
from mass_segmentation import batch_segmentation

# Пакетная обработка папки с изображениями
results = batch_segmentation(
    input_dir='./images/',
    output_dir='./results/',
    percentile_threshold=25
)
```

**Особенности:**
- Протестирован на автоматической разметке 2000 микрофотографий
- Сохраняет результаты в CSV/JSON формате
- Позволяет отслеживать качество сегментации для каждого образца

---

### 2. Профилометрический анализ глубины

#### `profile.py`
Методы обработки периодического сигнала с профилометра для определения глубины воздействия лазера.

**Функциональность:**
- Загрузка и обработка данных с профилометра
- Фильтрация и денойзация сигнала
- Определение максимальной глубины трека
- Расчет объема удаленного материала
- Построение профилей поперечного сечения

**Использование:**
```python
from profile import ProfileAnalyzer

analyzer = ProfileAnalyzer(profile_data)
max_depth = analyzer.get_max_depth()
profile_curve = analyzer.get_profile()
volume_removed = analyzer.calculate_volume()
```

---

### 3. Аппаратное обеспечение для сбора данных

#### Система автоматического фотографирования
*Код не входит в репозиторий, но является критической частью pipeline*

**Компоненты:**
- **Микроскоп** с автоматической фокусировкой
- **Подвижный стол** для позиционирования образцов
- **Автоматическая система захвата** изображений
- **Интеграция с лазерной установкой** для синхронизации экспериментов

**Функции:**
- Полная автоматизация процесса фотографирования
- Точное позиционирование образцов
- Автофокус для получения четких изображений на необходимой глубине
- Синхронизированная регистрация параметров лазера и изображений

---

## 📊 Хранилище данных

### Миграция на Hugging Face Hub

Проект перешел на открытую архитектуру хранения данных на **Hugging Face Hub** вместо локальной базы данных SQLite.

**Преимущества:**
✅ **Открытый формат** - данные доступны для визуального просмотра  
✅ **Быстрый доступ** - простое извлечение и дополнение данных  
✅ **DOI и цитирование** - фиксированная ссылка на датасет  
✅ **Удобство** - встроенные фильтры и поиск  
✅ **Масштабируемость** - легко добавлять новые образцы  

### Доступ к датасету

📌 **Основной датасет:**  
[https://huggingface.co/datasets/maximmikhalevich/laser_ablation_of_titanium_4](https://huggingface.co/datasets/maximmikhalevich/laser_ablation_of_titanium_4)

**Загрузка в Python:**
```python
from datasets import load_dataset

# Загрузка датасета
dataset = load_dataset("maximmikhalevich/laser_ablation_of_titanium_4")

# Просмотр первого образца
sample = dataset['train'][0]
print(sample.keys())  # laser_parameters, microscope_image, track_width, depth, etc.
```

### Правила просмотра на Hugging Face

⚠️ **Важно для корректного просмотра:**
- Используйте **фильтры отображения диапазона** датасета на платформе
- **НЕ просматривайте** сет страница за страницей
- Причина: большое количество медиафайлов может вызвать проблемы отображения

---

## 🚀 Начало работы

### Установка зависимостей

```bash
# Основные библиотеки для обработки изображений
pip install opencv-python pillow numpy

# Для работы с датасетом на Hugging Face
pip install datasets huggingface_hub

# Дополнительные библиотеки для анализа данных
pip install pandas matplotlib scikit-image
```

### Пример использования

```python
from datasets import load_dataset
from segmentation_1 import segmentation
from profile import ProfileAnalyzer

# 1. Загрузка датасета
dataset = load_dataset("maximmikhalevich/laser_ablation_of_titanium_4")

# 2. Работа с первым образцом
sample = dataset['train'][0]
image = sample['microscope_image']
laser_params = sample['laser_parameters']
depth = sample['track_depth']

# 3. Повторная сегментация для проверки
seg = segmentation(image, percentile_threshold=25)
print(f"Ширина трека: {seg.avg_width} мкм")
print(f"Глубина: {depth} мкм")
```

---

## 📈 Статистика датасета

- **Всего образцов:** 2000+
- **Материал:** Титан
- **Формат изображений:** JPG/PNG
- **Разрешение:** 8-битные черно-белые изображения
- **Диапазон мощностей лазера:** [уточнить]
- **Диапазон частот:** [уточнить]
- **Различные скорости развертки:** да

---

## 🔗 Связь с другими модулями

### Solvers → Database
Солверы генерируют физически корректные предсказания температурных полей, которые используются для валидации и расширения экспериментального датасета.

### Database → PINN Model
Подготовленный датасет используется для дообучения (fine-tuning) Physics-Informed Neural Networks в ветке `PINN_3D_real_parms`.

---

## 📝 Протокол сбора данных

1. **Подготовка образцов** - образцы титана готовятся и закрепляются на столе
2. **Установка параметров лазера** - конфигурация режима лазерного воздействия
3. **Автоматическое фотографирование** - микроскоп фотографирует результат
4. **Профилометрия** - измерение глубины трека
5. **Сегментация** - автоматическая обработка изображений
6. **Структурирование** - формирование записи датасета

---

## ⚠️ Важные замечания

### Параметр `avg_width`

Параметр **`avg_width`** (усредненная ширина) является более надежной метрикой, чем `track_width`, особенно для неровных или искривленных краев трека.

- `track_width` = разность крайних точек (может быть завышено при неровности)
- `avg_width` = среднее по всем слоям (более стабильно)

**Рекомендация:** При анализе данных предпочитайте `avg_width`.

### Погрешность сегментации

Алгоритм сегментации протестирован и показал погрешность:
- **< 1%** при сравнении с оптическими измерениями
- Результаты валидированы на 2000 образцах

---

## 📚 Дополнительные ресурсы

- [Hugging Face Datasets Documentation](https://huggingface.co/docs/datasets)
- [OpenCV Documentation](https://docs.opencv.org/)
- [PIL/Pillow Documentation](https://pillow.readthedocs.io/)

---

## 🤝 Контрибьютинг

Для добавления новых образцов или улучшения обработки:

1. Обработайте новые микрофотографии с помощью `segmentation_1.py` или `mass_segmentation.py`
2. Выполните профилометрические измерения
3. Создайте pull request с новыми данными
4. Данные будут добавлены в датасет на Hugging Face

---

## 📄 Цитирование

При использовании этого датасета в научных работах, пожалуйста, ссылайтесь на него через DOI на Hugging Face Hub или используйте следующий формат:

```bibtex
@dataset{laser_ablation_2024,
  title={Laser Ablation of Titanium: PINN Training Dataset},
  author={ILT-ITMO},
  year={2024},
  publisher={Hugging Face Hub},
  url={https://huggingface.co/datasets/maximmikhalevich/laser_ablation_of_titanium_4}
}
```

---

## 📧 Контакты

Для вопросов о датасете или алгоритмах обработки создавайте issues в [основном репозитории](https://github.com/ILT-ITMO/LaserProcessing).
